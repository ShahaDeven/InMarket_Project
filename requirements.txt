# ──────────────────────────────────────────────────────────────
# InMarket — dependencies
# Grouped by component. Verified against Python 3.11.
# ──────────────────────────────────────────────────────────────

# --- MCP server (mcp_server/) ---
fastmcp==3.4.0
httpx>=0.27.0
python-dotenv>=1.0.1

# --- LLM agent (agent/) ---
langgraph>=0.2.60
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-mcp-adapters>=0.1.0

# --- Frontend (frontend/) ---
flask>=3.0.0
markdown>=3.6
