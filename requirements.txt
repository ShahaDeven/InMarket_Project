# ──────────────────────────────────────────────────────────────
# InMarket — dependencies (pinned for reproducible, auditable builds)
# Versions verified working on Python 3.11.
# Scan for known CVEs with:  pip-audit -r requirements.txt
# (see requirements-dev.txt)
# ──────────────────────────────────────────────────────────────

# --- MCP server (mcp_server/) ---
fastmcp==3.4.0
httpx==0.28.1
python-dotenv==1.2.2

# --- LLM agent (agent/) ---
langgraph==1.2.4
langchain-core==1.4.0
langchain-anthropic==1.4.4
langchain-mcp-adapters==0.2.2

# --- Frontend (frontend/) ---
Flask==3.1.3
Markdown==3.10.2
nh3==0.3.6          # HTML sanitizer (allowlist) for LLM output — OWASP LLM05/A03
gunicorn==26.0.0    # production WSGI server — used in the Docker image (Linux);
                    # local dev on Windows uses `python -m frontend.app` instead.
